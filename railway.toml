[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.dev"

[deploy]
startCommand = "set -x; echo BOOT_START $(date -u); nginx; echo NGINX_RC=$?; pnpm run pm2 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckTimeout = 600
